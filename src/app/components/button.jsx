const Button = ({ text }) => {
  return (
    <>
      <div className="adv-button">
        <span>{text}</span>
        {/* <span>Button</span> */}
      </div>
    </>
  );
};

export default Button;
